# shellcheck shell=bash

post_install() {
  printf '%s\n' 'Run "gsr-replay setup" as your desktop user to complete onboarding.'
}

post_upgrade() {
  printf '%s\n' 'Run "gsr-replay doctor" before restarting an active replay buffer with "gsr-replay restart".'
}
